export default {
  plugins: {
    // 1. Tailwind yapılandırmasını işler
    tailwindcss: {},
    
    // 2. CSS kodlarını tarayıcı uyumlu hale getirir (Vendor Prefixes)
    autoprefixer: {},
    
    // Opsiyonel: Gelecekte CSS boyutunu optimize etmek için 'cssnano' eklenebilir
  },
}